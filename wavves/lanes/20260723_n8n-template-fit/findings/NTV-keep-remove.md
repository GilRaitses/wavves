# NTV KEEP / ADAPT / REMOVE matrix

| Meta | |
|---|---|
| charge_id | NTV-W1d |
| tip_base | `de75b4c4118c78dcc0164fdaa916bbc53f99225f` |
| status | proposal (not locks) |
| sibling_inputs | W1a/b not yet on disk; grounded from `README.md` core ideas + skills table, `waveset.md` Grounding/Acceptance, FR `20260723_n8n-template-fit.md` |

## Root cut

Full Cursor plugin (slash skills, marketplace install, `wavves/` markdown home) cannot be the n8n template. Publishable shape is a single practical workflow (or small workflow family) that carries only ideas that survive n8n review: sticky-documented graph, credentials, Set Fields, original work, SEO title later.

## Matrix

| surface | KEEP / ADAPT / REMOVE | n8n-native mechanism (ADAPT required) | one-line why |
|---|---|---|---|
| Alignment packets / lane home | ADAPT | Yellow Sticky Note for scope + acceptance; Set Fields (or item JSON) for locked facts, file/API boundaries, acceptance checks | Bounded charter stays; Cursor lane directories do not. |
| Bounded waves / fan-out triad (O0, orch, charge) | ADAPT | Manual/Webhook trigger as O0; parent workflow Wait + Merge (or Execute Workflow) as orch; parallel AI Agent or sub-workflow nodes as charges | Fan-out and role split map to graph shape; Cursor Task UI does not. |
| Disk gate captures | ADAPT | After a check node: append pass/block row to Google Sheets or n8n Data Table; optional Write Binary / Drive for logs | Evidence must persist outside chat; repo `gate-captures/` paths are Cursor-disk. |
| Proof-before-accept | ADAPT | Named HTTP Request or Code check, then IF/Switch before final output; Error Workflow on fail | Product ACCEPT needs a runnable gate node, not process-only close. |
| Paragraph tunnel | ADAPT | Sub-workflow: one Set text field → AI Agent adversarial pass → Code fixture checks → capped rewrite loop → IF pass/fail | Mid-render structural gate can be a text subflow; voice-clone and Cursor outbound framing drop. |
| Rotation / term identity | ADAPT | Data Table (or static data) holding monotonic `term_id`; Set Fields on handoff; IF reject when item term ≠ current term | Keep stale-instruction detection; drop moderator chat paste and `/mod-rotate`. |
| Standing home (AGENTS, INDEX, registry, step-log) | REMOVE / Cursor-only | n/a | Fresh-instance markdown hydration under `wavves/` has no honest n8n home equivalent; do not ship as template. |
| Authority propagation / mod-decide locks | ADAPT | Human approval (Form/Wait) writes Set Fields or Sheet row; later branches and Execute Workflow inputs read those locks only | Settled decisions must propagate as data, not as Cursor mod-decide slash sync. |
| Router `/wavves` and playbooks | REMOVE / Cursor-only | n/a | Playbook router is a Cursor slash skill surface, not an n8n node. |
| `/mod-check` slash skill | REMOVE / Cursor-only | n/a | Adversarial check idea may ADAPT as AI Agent branch above; the slash skill itself is Cursor-only. |
| `/mod-decide` slash skill | REMOVE / Cursor-only | n/a | Lock-walking UX is Cursor chat; locks themselves ADAPT under authority propagation. |
| `/mod-rotate` slash skill | REMOVE / Cursor-only | n/a | Thread handoff paste is Cursor-only; term identity ADAPTs under rotation row. |
| Cursor plugin install / marketplace (`/add-plugin`, local copy) | REMOVE / Cursor-only | n/a | Template library ships workflow JSON, not a Cursor plugin package. |
| Sticky notes (yellow full description + step stickies) | KEEP (BUILD must) | n8n Sticky Note nodes (mandatory in any later BUILD) | Guidelines require them for review; assume present if a workflow ships. |
| Credentials (no hardcoded API keys; strip personal IDs) | KEEP (BUILD must) | n8n Credentials store on HTTP/AI/API nodes | Ban risk and review fail if keys or personal IDs are inlined. |
| Set Fields for visitor-facing config | KEEP (BUILD must) | Set node (or equivalent) at setup surface | Guidelines require configurable fields instead of buried constants. |

## Counts

| verdict | rows |
|---|---|
| KEEP (BUILD must) | 3 |
| ADAPT | 7 |
| REMOVE / Cursor-only | 6 |
| **total** | **16** |

## Publishable workflow ideas (matrix outcomes only; not BUILD)

Name only. No JSON. For NTV-INT / mod-decide later:

1. **Coordinate bounded parallel research charges with evidence gates.** Trigger a wave of parallel AI Agent sub-workflows, merge returns, run a proof check, write pass/block evidence to a Sheet, emit a short rollup.
2. **Lock then execute.** Form/Wait captures operator locks into Set Fields; downstream agents read locks only; no re-litigation branch.
3. **Optional narrow add-on.** Paragraph-tunnel sub-workflow on one outbound text field (only if decide keeps that use case).

Do not propose "port the Cursor plugin" or a slash-skill catalogue as the template.

## Honesty notes

- Slash skills (`/wavves`, `/mod-check`, `/mod-decide`, `/mod-rotate`, and sibling leaves) stay REMOVE / Cursor-only. Do not invent them as fake n8n nodes.
- `wavves/` disk home stays REMOVE / Cursor-only. A later Data Table "run registry" would be a new thin ADAPT, not a KEEP of AGENTS/INDEX/step-log.
- W1a inventory and W1b guidelines checklist were absent when this matrix was written; reopen rows only if those findings contradict path evidence, via orch rollup / O0, not by silent edit here.
