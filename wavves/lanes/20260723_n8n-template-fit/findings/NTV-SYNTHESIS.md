# NTV-SYNTHESIS (O0 remediation)

| Meta | |
|---|---|
| author | O0 (fail remediation; NTV-INTa never wrote files) |
| grounded_on | W1 pack PASS `gate-captures/NTV-W1.md` |
| tip | `de75b4c4118c78dcc0164fdaa916bbc53f99225f` |
| status | draft for mod-decide; not locks |

## One-sentence job

Run a bounded wave of parallel research agents, merge their returns, and
block final accept until a named proof check writes pass/fail evidence to a
durable store.

## SEO title draft

`Coordinate parallel research agents to a proof-gated accept in n8n`

(Form: Action verb + thing + to/on/in/from + where. Finalize at BUILD copy.)

## Originality wedge

**Governed wave accept** (alignment packet + role triad + independent gate
evidence). Not "first orchestrator." Competitors already cover CEO/manager
agents and async fan-out (W1c: 7504, 7158, 8578, blog multi-agent systems).

## Node-family sketch (not JSON)

Manual Trigger or Form → Set Fields (locks + proof_job) → yellow Sticky
(full description) + step Stickies → Orchestrator path: Execute Workflow /
AI Agent charges in parallel → Merge → named proof HTTP/Code check → IF →
append gate row (Sheet or Data Table) → rollup output. Credentials on AI/HTTP
only. No Cursor paths, no slash skills.

## KEEP / REMOVE summary (from W1d)

| KEEP (BUILD must) | ADAPT | REMOVE / Cursor-only |
|---|---|---|
| Stickies, credentials, Set Fields | Alignment packet, triad/fan-out, gate evidence, proof-before-accept, lock propagation; optional term_id / paragraph-tunnel | `/wavves` + mod-* slash skills, `wavves/` standing home, plugin install |

## Description section bullets (~200 words later)

- **Who's it for:** Operators who need parallel AI research with a hard accept
  gate, not a one-shot chat merge.
- **How it works:** Set locks → fan out charge sub-workflows → merge → run
  proof check → write evidence → emit rollup or block.
- **How to set up:** Import JSON → attach LLM credentials → fill Set Fields →
  point evidence store → run Manual Trigger once.
- **Requirements:** n8n with AI/LangChain nodes; LLM credential; Sheet or Data
  Table (per decide); no community-node dependency planned for v0.
- **How to customize:** Swap charge prompts, change proof check, add Wait for
  human lock approval.

## Pricing

Free first submit (lane lock).
