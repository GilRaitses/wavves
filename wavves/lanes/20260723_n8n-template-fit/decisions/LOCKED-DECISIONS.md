# NTV — Locked decisions

```text
Locked decisions (do NOT reopen):
- NTV-JOB = A — Free template job: bounded parallel research → proof-gated accept (not lock-only, not paragraph-tunnel identity).
- NTV-PACK = B — Parent orchestrator + Execute Workflow charge sub-workflows (not a single flattened graph).
- NTV-GATE-STORE = B — Pass/block evidence in n8n Data Table (not Sheets/Drive for v0).
- NTV-LLM = C — Generic LangChain chat-model credential; importer picks provider.
- NTV-V0-SCOPE = A — Core only (packet + fan-out + proof gate + Data Table); no term_id, no paragraph-tunnel in first Free template.
- First submit pricing = Free (lane lock).
- Slash skills, wavves/ standing home, plugin install = REMOVE / Cursor-only.
- Sticky notes, credentials (no hardcoded keys), Set Fields, originality, SEO title form, personal-ID strip = BUILD musts.

Grounding (already verified — do not rediscover):
- Repo has zero n8n workflow JSON at tip de75b4c; template is greenfield.
- W1 PASS: gate-captures/NTV-W1.md; KEEP/REMOVE matrix and honesty PASS.
- Library gap name: governed wave accept (not another CEO/orchestrator demo). Cites: n8n.io/workflows/7504, 7158, 8578; blog.n8n.io/multi-agent-systems/
- Example 4817 is description shape only; do not copy its graph.
```

proof_required: yes
proof_job: After parallel charges merge, a named proof check must write a pass or block row to the Data Table before the accept/rollup path can emit success.
proof_reference: Data Table gate row schema documented in BUILD stickies; none until BUILD lands the table columns.
chrome_freeze: freeze Cursor plugin skills/, wavves/ standing home (except this lane), index.html; proof-serving allowlist = n8n workflow JSON pack + sticky text + Set Fields config + Data Table schema docs under the BUILD lane home.
visual_accept: no — n8n canvas/stickies are reviewed in n8n UI / exported workflow image if community nodes appear; no wavves_build visitor DOM Proof. Rationale: deliverable is n8n template JSON, not the GitHub Pages homepage.
