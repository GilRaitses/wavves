# PASB-INT — single editor convergence

```yaml
wave: PASB-INT
role: wave_orchestrator (single editor)
tip: de75b4c4118c78dcc0164fdaa916bbc53f99225f
git: none
```

## Charge returns reviewed

W1a/b/c/d all PRESENT. Trigger tables for closed all-standing phrases and
bare-shrug non-widen match across `proceed.md`, `skills/shrug/SKILL.md`, and
`skills/wavves/SKILL.md`. No playbook/leaf edit required.

## INT edit (docs AC gap)

FR Acceptance asks README / usage / **index** to list `/shrug` and
all-standing. W1d owned README + usage only. Orch INT patched `index.html`:

| surface | change |
|---|---|
| Commands table | `/shrug` row |
| Playbooks proceed row | closed all-standing phrases; bare non-widen |
| Usage cards | proceed pre shows all-standing; new `/shrug` card |

No other shared-file collisions. COMMIT-AUTH-GRAIN=C and SCOPE-FALLBACK=A
unchanged (playbook already carries them from W1a).
