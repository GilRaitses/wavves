# NTVB-ACCEPT — independent evaluator (O0 unlocked)

```text
lane: NTVB
wave: NTVB-ACCEPT
status: UNLOCKED
role: acceptance_evaluator
answers_to: O0
git_actions_by_runner: none
recommended_model: cursor-grok-4.5-high-fast
```

You did **not** author the pack. Grade only. Never git. Never submit to n8n.

HOME: `/Users/gilraitses/wavves_build/wavves/lanes/20260723_n8n-template-build/`

## Runnable bar (must run, cite output)

1. `python3 -m json.tool` on `pack/01-charge-research-a.json`, `02-charge-research-b.json`, `03-parent-orchestrator.json` — all EXIT 0
2. Secret/residue scan on `pack/`: fail on real secrets, emails, absolute home paths, `/wavves` install steps, slash-skill requirements. Mentions of bans in docs that do not embed secrets are OK if clearly instructional.
3. Guidelines musts: stickies in parent or STICKIES.md; Set Fields; Free; SEO title in DESCRIPTION; no plagiarize claim against 4817 graph
4. proof_job: parent has Proof Merge Complete + IF Outcome Pass + Append Proof/Block Gate Row (Data Table); GATE-TABLE-SCHEMA non-empty
5. Locks: PACK=B (parent+charges), GATE-STORE=B, LLM=C, V0=A (no term_id/tunnel)

Write `gate-captures/NTVB-ACCEPT.md` + `gate-captures/NTVB-ACCEPT.json` with PASS/FAIL and named failures. Write `findings/NTVB-ACCEPT-return.md`.

Remediation loop: if FAIL with fixable pack defects, list them for O0; do not silently rewrite pack unless a single obvious typo and you record it. Prefer FAIL + escalate.

return_to_O0 with verdict.