# RTH-W1e return

```text
charge: RTH-W1e
role: charge_worker (adversarial)
model: cursor-grok-4.5-high-fast
tip: 7aecdfb50d8d6fc53fa00a08b021e4cd9926aa67
```

## Result

**FAIL**

Honesty/overclaim in shipped prose would poison synthesis if RTH-INT echoes
README/wavves-init DS borrowings as isomorphism, treats empty `rotations/` as
live continuity, upgrades hub/spoke to "graph theory," or claims ROLE-COLLAPSE
already closed. Orch gate: pause for O0 before RTH-INT (waveset GATED).

Deliverable bar met: ≥3 evidenced risks + `synthesis must NOT say` section
present in findings. FAIL is the orch honesty gate, not a missing-file fail.

## Paths written

| path | role |
|---|---|
| `findings/RTH-adversarial.md` | owned findings (risks R1–R7 + bans) |
| `findings/RTH-W1e-return.md` | this return |

## Risks counted (for ACCEPT checklist)

1. R1 — unlabeled DS "proven designs" / false isomorphism (init §4 + README)
2. R2 — empty `rotations/` vs continuity / enforcement claims (INDEX + AGENTS + init quality bar + mod-rotate "newest file")
3. R3 — graph/star theory upgrade without graph ops (lane framing vs skill text)
4. R4 — RotatE name collision
5. R5 — ROLE-COLLAPSE still live in AGENTS/README vs SFR/WOF locks
6. R6 — public README overclaim vs thin AGENTS
7. R7 — missing first-rotation template fence in mod-rotate

## Gaps

- Did not author sibling W1a–d findings; no cross-lens reconcile.
- Did not fetch external Raft/RotatE papers (house paths only; RotatE ban is
  lane-locked, not a literature review).
- Did not remasure foreign pax pins (out of scope; illustration only).
- Leave-act vocabulary audited via WOF FR + SFR waveset only; not applied to
  live AGENTS (expected: FR not BUILD-landed).

## Git

No git. Commit file list for O0 only: the two paths above.
